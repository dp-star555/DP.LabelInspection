# 实物条码灰度损失漏检诊断

## 复现与证据

使用用户指定原图`NG_img_20260831163718773.jpg`和实际保存的ROI/阈值回放，原文件不修改。开发产物包含私有标签内容，不随分发包公开。

- 左条码ROI `[133,722,1096,144]`，原二值化Otsu阈值为132。
- 同一条内部完好段灰度中位数约0，受损段中位数约72，75分位约120。
- 这些已经明显变浅、但灰度未超过132的像素仍被旧算法当作黑墨。
- 原图点`(587,806)`没有旧候选框覆盖；CLI回归明确FAIL。此处灰度105。
- 合成黑条内部填入灰度90的24像素区域，也能复现旧检查完全漏掉墨色损失。

## 修改

保留二值孔洞检查，增加原图灰度证据：

1. 在已定位条内部，按每列沿条高的10%低分位取得深色自参考。
2. 像素相对该参考变浅达到可用对比度的配置比例，且至少差20灰度级，加入损失掩膜。
3. 与原二值缺墨做并集，再做连通域和面积/局部比例判断；不重复累计同一像素，不修改或修补输入图。
4. 包含新增灰度损失的连通域输出`barcode_ink_loss`，记录总面积及其中新增灰度像素数；纯二值孔洞保留`barcode_missing_ink`。

配置位于“条码打印质量”：

- `DetectInkLoss=True`：启用一维条内墨色损失检查。
- `MinimumInkLoss=0.25`：相对本列深色参考的对比度损失比例，不是整块ROI差异比例。绝对差至少20灰度级。
- 最小面积、局部面积比例与条边容差继续生效。旧配方缺少新字段时采用默认值；显式关闭能够持久化。

## 回放结果与限制

同一原图、同一ROI、原有面积/比例/条边阈值不变：选定漏检点从FAIL变为PASS。左码候选像素面积由578变为1891；这只说明更多灰度损失进入候选，不是精度/召回率，也不能据此宣称1891像素全为真缺陷。对比图在`artifacts/user-ng-ink-loss-comparison.png`。

仍有局限：端部圆角与条边正常波动可能误报；大范围均匀变浅、整条缺失仍可能进入自身参考；正常照明变化需采集控制及工艺校准。没有完整人工像素标注，未声称所有可见缺陷均已覆盖。新规则目前仅用于一维条码，不静默推广为QR灰度检测。

## 可重复检查

先用`BarcodeRegression`对原图及配方输出当前日志，再执行：

```powershell
python tools/check_barcode_point.py <回放UTF-8日志> 587 806
```

该脚本仅验证指定点的候选框覆盖，不等于像素掩膜覆盖。合成回归另验证水平/垂直灰度损失、24像素面积、关闭/阈值控制、均匀灰条不误当作局部损失、旧配方默认与显式关闭持久化。
