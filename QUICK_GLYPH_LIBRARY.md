# 多图补录与人工分割制库

这是**单字外观参考库**，不是OCR训练数据集，也不记忆上一张标签的整段内容。**不要求一次收齐字符，不要求一张图片包含所有字符。可以随时保存部分字符，下次打开同一字库继续补录。**

运行当前源码的`start-ocr.cmd`或`start.cmd`，点击工具栏 **多图制库**；也可从“单字库 → 多图补录 / 手工分割”进入。旧dist尚未重发。

## 四个明确步骤

### ① 选择 / 新建目标字库

点击左上角 **＋ 新建字库**，填写字体/工艺类别名称；已有库直接选中即可。

右侧显示该库的当前修订、已收录字符、缺字和已保存参考预览。62个ASCII字母/数字只是支持范围，**不是必须填满的任务清单**。字体、宽窄、明显不同字号或工艺建议分库。

### ② 当前图片：自动候选或人工补字

点击 **导入 / 下一张图**。

- 自动：选择“框选文字行”，框出完整横向单行，点击“识别并分割”。OCR不准确时，填写重分割用文字再点击“按文字重分割”。整行文字输入不会自动修改表格里的单字标签。
- 人工：切换“手工补画字块”，在原图拖出一个字或一对粘连字。**自动结果为0、没有OCR模型、甚至宿主未连接自动提取服务，也可手工补画。**
- 自动已有一些候选时，补画会追加，不要求推倒整行重来。
- 候选默认不勾选。在“填写单字”列修改字符，查看图块及原图坐标。

#### 两个字符粘在一起怎么分

1. 如果没有候选，先用“手工补画字块”框住这两个字符。
2. 在下方表格选中该块。
3. 切换 **手动切开（点切线）**，将鼠标移到分界位置。橙色虚线表示可切，红色表示两侧宽度不足。
4. 点击后分成两个字块。**两侧标签均清空**，请分别填写并核对；不是把原标签复制两遍。
5. 切错可“撤销调整”，重新选切线；也支持重做。

手动切割按原图整数X坐标划分已有图块，不擦除桥接墨迹，不插值、不拉伸。切线两侧至少各4像素。当前支持垂直直线，不支持曲线/斜线像素分界或智能修复被切伤笔画。

#### 边界不全 / 混入邻字怎么调

- 切换 **调整候选框**，点击框后拖动框内移动，或拖八个控制点调整边界。
- **精确改框 X/Y/宽/高**可填写原图整数坐标。
- “删除选中候选”删除当前图里的错误字块；撤销/重做保留最近20次当前图调整。
- 调整边界会从原图重新裁取，可能重新包含邻字墨迹，必须重新核对；不能把有缺陷的字块裁一裁就当成合格参考。

### ③ 核对并加入待入库清单

1. 点击“只选库中缺字”，或手工勾选需要的候选。
2. 核对单字标签和完整字形，再勾选“已核对所选字符和完整字形”。
3. 点击 **加入待入库清单 →**。

右侧清单显示字符、图块和来源图片。默认跳过库中已有、清单已有以及本次重复字符，并显示跳过项；**一个已有B不会再阻止新C加入**。

随后可导入下一张图继续补字。换图、重框行、重新自动分割只替换当前图候选，**不清空已暂存的清单**。来源哈希、图块像素和来源图名跟随每个条目，不会被下一张图覆盖。

暂存的是已核对的独立快照。继续调整左侧同名字块不会暗改右侧；确需更新时，先从清单移除旧项，再核对加入。清单绑定一个目标字库，有清单时不能无提示切换字体类别。

### ④ 随时保存，随后继续补录

点击 **保存清单到字库**，这一批发布一个不可变新修订，原有字符和历史修订保留。保存成功显示修订和已收录数量；现在就可以继续导入更多图片，或下次再补。

- 清单是当前窗口内的暂存，**不是自动保存到磁盘**。关闭有待存/未暂存候选的窗口会提醒；长期工作请分批保存。
- 如果需要替换已收录参考，显式勾选“本次允许替换库中已有字符”，再选择对应候选暂存/保存。历史像素仍可读取。
- 版本冲突或写入失败保留清单；刷新目标字库、处理重名项后重试，不会半批写入。
- 配方仍绑定原类别ID/修订，字库新修订不会自动改变配方。

## 检测报“参考整图尺寸不一致”

这是整图模板配置检查，不是自建单字库损坏。示例/之前载入的参考整图可能仍然保留，换了尺寸不同的待检图会触发检查。

- 仅做单字库检查：点击检测工具栏 **无整图参考（可用单字库）**，ROI及字库绑定保留；按需检查绑定的类别/修订。
- 需要固定区域整图模板差异/模板平移：载入与待检图同尺寸、内容布局匹配的参考整图，不要拉伸单字图块。
- 工具栏显示当前模式、参考/待检尺寸及尺寸冲突；没有偷偷丢弃旧参考，也没有放宽SDK模板校验。
- SDK使用`EInspectionMode.Free`和`Reference=null`可独立执行单字库比较。

新增3项测试及WinForms模式切换探针；164项/框架通过。日志`artifacts/reference-mode-verification.log`。

## 参数看不懂时

“编辑ROI/规则”已提供26项中文参数名、中文是/否和枚举、底部固定说明区。选中参数可查看用途、单位、范围与调节影响。详见[ROI_PARAMETER_GUIDE.md](ROI_PARAMETER_GUIDE.md)。

## 检测结果策略更新

选定ROI字库后，分割失败、缺参考字或未完整执行比对按**NG**处理；同一ROI只报一个F汇总，内部原始子项完整保留。阻断原因不计作印刷缺陷点。详见[ROI_EVIDENCE_POLICY.md](ROI_EVIDENCE_POLICY.md)，当前完整验证170项/框架。

## 单字及算法边界

- `A–Z/a–z/0–9`，区分大小写；每字符每类别/版本一个参考，宽高各4–512原始像素。不自动纠正O/0、I/1，不支持汉字或标点条目。
- 不使用带红框标注的检测截图做参考。自动像素归属块可能隔离邻字墨迹，未必等于简单矩形裁图；手工补框/改框则明确使用原图矩形裁取。
- 自动单行ROI宽不超过6000、高不超过512，文字最多128个可打印ASCII字符；不进行整页多行自动布局。
- 自动薄粘连补切只在至少3个原组、有偏宽块及薄墨迹间隙时尝试，最多补4切，有固定预算。结果标记需复核，不是合格判定。
- 历史同批`WF675907`10张自动回放：5张原路径、4张需复核候选、1张拒绝。这是候选覆盖，不是准确率；**本次加入的是自动拒绝后的人工编辑能力，没有宣称自动算法已能解决所有粘连。**
- 正式检测的保守分割策略没有放宽。人工切线不认证切割正确性或印刷外观质量。

## SDK与生命周期

- `Core.GlyphDraftSession`集中处理当前图候选、补框/改框/切割/标签、20步撤销重做、跨图清单、重复项提示和原子发布；不依赖UI、OpenCV或存储路径。
- `GlyphQuickBuilderControl.AttachServices(manager, extractor: null)`可仅做人工制库；自动服务仍使用`IGlyphCandidateService`，与正式检测共用`InspectionEngine`串行门。
- 原有`SaveSelected()`保留为“暂存选择项后保存”的兼容快捷方式；新增`StageSelected()`、`SavePending()`、`PendingCount`、`AddManualCandidate()`、`SplitSelected()`。
- `IGlyphBatchLibraryManager.PutGlyphs`仍严格拒绝未经许可的重名及陈旧修订；缺字筛选发生在制库模块，而不是放宽存储合同。
- 来源记录：像素SHA256、来源文件名（不含目录）、原图尺寸、最终图块坐标、操作类型、原暂定字符、需复核切割标记、人工确认标记。人工改标签不覆盖原暂定身份。
- 正在提取时关闭先取消并等待，再释放UI。未发布清单不会影响检测任务。
- 页面目前为原生WinForms，net48/net8；WPF尚无同等页面，可复用Core会话与Contracts接口。不是WindowsFormsHost。

## 验证

- 新增失败回放：旧页面勾选“已有B + 新C”，得到`Character already exists: B`且整批失败。日志`artifacts/library-increment-red.log`、`artifacts/library-increment-red.error.txt`。
- 新增13项会话回归：跨图像素/来源、分次保存、重名跳过、零自动结果人工分割、BGR切割像素守恒、切线拒绝不损坏历史、暂存原子性、调整不改已暂存快照、陈旧修订保留清单、类别绑定、显式替换、原始身份保留、已收录小图块不阻碍新字。
- 原生WinForms两框架探针：混合重复/新增的实际页面保存；无自动服务时窗口消息补框、点击切线、拖边界、撤销/重做；两张图片暂存3字并一次发布，原有字符与各来源像素均保留。
- 全部测试：**161/框架**，0失败/跳过，Release 0警告/错误；原有实际OCR/OpenCV与原生WinForms/WPF流程通过。
- 日志：`artifacts/library-editor-verification-final.log`。
- 截图：`artifacts/winforms-net48.png.quick-library.png.multi-image.png`、`artifacts/winforms-net8.png.quick-library.png.multi-image.png`。截图使用合成测试素材，不分发生产图。
- WPF原有渲染/工作流通过不代表新页面已支持WPF，也不代表物理WPF鼠标路由已验证。
