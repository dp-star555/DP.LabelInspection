# 局部块异常检测（PatchCore式，仅用良品训练）

质量方法B：与方法A（按类型的规则质检：分割+单字比较、条码/QR边缘带检查、固定/空白墨迹比较）并列，可单独或同时启用，同时启用时任一NG即NG。
只用良品训练，不需要缺陷样本，也不需要字库，适用于文字、条码、固定和空白ROI。两种模式：

- **整ROI模式**：整个ROI一个模型，适合内容固定的区域（标题、固定文字、图形、条码外观），不需要OCR。
- **逐字符模式**：内容可变的文字（序列号、批号等0-9/a-z/A-Z自由组合）按字符训练，每个字符一个模型，检测时按分割逐字比较，见下文“逐字符模式”。
实现为手工特征的PatchCore式方法（无深度学习运行库依赖）；特征提取可替换，以后可换成CNN特征（方案B）。

## 原理

1. 每个ROI裁图按本图纸色（98%分位）换算为墨量（0纸白–1满墨），抵消整体亮度变化，整体褪色仍表现为墨量下降。
2. 每个位置取8×8原尺度块和同位置的1/2尺度8×8上下文块作特征。
3. 两种记忆方式：
   - **位置相关**（`localRadius`=3）：保存各良品裁图，检测块只与良品**同一位置±3像素**内的块比较；纸白块也参与，良品有墨处实际无墨同样得高分。适合内容固定的ROI。
   - **与位置无关**（`localRadius`=null）：良品块经贪心k中心核心集压缩为记忆库，检测块与库中任意块比较。适合可变内容（序列号、条码）。
4. 得分=到最近良品块的距离；阈值用留一法标定（每张良品用其余良品评分，取最大值×余量1.5），只有一张良品时用平移1像素并轻度模糊的增强图。
5. 超阈值区域按连通域输出`patch_anomaly`缺陷（原图坐标、面积、峰值倍数）及热力图（128=阈值）。裁图边缘一个块宽只作上下文。

`RegionAnomalyDetector.DefaultOptions(region)`：固定/空白ROI及带引导值的文字ROI用位置相关模式，其余用与位置无关模式。

## 特征来源：手工特征或CNN骨干网络

- **手工特征**（`OpenCvPatchAnomalyDetector`）：墨量平面上的8×8块加1/2尺度上下文，无外部模型。
- **CNN骨干网络**（`OpenCvCnnPatchAnomalyDetector`）：PaddleOCR PP-OCRv4文字检测模型（Apache-2.0，经PyPI上的RapidOCR包分发）截取的骨干网络，
  取1/4与1/8级中层特征（3×3邻域平均后拼接，144维），裁图先放大2倍。该网络在文字图像上训练，比ImageNet骨干更贴近印刷字符。
  - 由OpenCV DNN运行（已随OpenCvSharp发布，Windows x64/x86运行库均含所需的`dnn_readNetFromONNX`、`dnn_Net_forward3`等导出），不增加依赖。
  - 骨干网络文件约110 KB，用DP.Vision的`tools/export_ppocr_backbone.py`从PyPI上的`rapidocr_onnxruntime==1.4.4`生成（可逐字节复现，SHA-256 `dfcac0b9…e2bf`）。
  - 模型记录骨干网络哈希与放大倍数，换骨干网络或倍数后旧模型被拒绝，需重新训练。
  - 位置相关模型只保存灰度裁图，首次检测时按同一骨干网络重算特征并缓存（单个ROI约0.35–0.65 MB；若保存特征网格则为13–24 MB）。

其他开源组件的取舍：ImageNet预训练骨干（torchvision/timm）需要从Hugging Face/PyTorch下载权重（当前环境不可达，现场可自行导出同样两输出的ONNX使用）；
ONNX Runtime可替代OpenCV DNN，但会增加原生依赖，而OpenCV DNN与ONNX Runtime对本骨干网络的输出最大相差2×10⁻⁵；
FAISS没有官方.NET绑定，记忆库规模下OpenCV暴力匹配已足够；Anomalib为Python训练框架，不直接用于.NET运行时。

## 接入配方：异常模型库与ROI绑定

与单字库相同的管理方式：

- **模型库**（`IAnomalyLibraryManager`，本地实现`AnomalyLibraryStore`，`InspectionStore.AnomalyLibraries`与字库共用存储根目录）：
  新建、导入/导出（导出文档内嵌模型字节）、归档/恢复；每次添加/替换/删除模型都发布新的不可变版本，按调用方基于的版本拒绝过期编辑。
  版本文档`anomaly-libraries/<id>/revisions/<n>.json`只记录元数据（特征来源、模式、裁图尺寸、边距、步长、最小面积、阈值、良品数、标定说明、来源），
  模型字节按SHA-256保存为`models/<sha>.bin`，多个版本共享；读取时校验哈希。
- **ROI绑定**：`InspectionRegion.Anomaly = AnomalySettings(模型库ID, 固定版本, 模型键)`，模型键缺省为ROI名称；`Tasks.DetectAnomaly`为B开关，`Tasks.CheckQuality`为A开关。
  重新训练发布新版本后，配方不会自动升级，需重新绑定。
- **训练**：`IAnomalyModelTrainer`（运行时实现为`RegionAnomalyDetector`）用已与配方对齐的整张良品图为ROI训练，返回`AnomalyModelEntry`，再`PutAnomalyModel`发布。
  WinForms工作台“单字库”组中的“异常模型库(B)”：勾选ROI、添加良品图（最近一次检测为OK的当前图自动加入）、训练并发布，关闭时可一键把训练的ROI绑定到新版本并启用B。
  良品图显示在画布上，所选ROI的框可直接编辑：拖动框内部只移动**本图**上的位置（良品位置略有偏差时对齐用，训练前按此平移该图）；
  拖动边/角或在框外拖动重画会改变该ROI的**尺寸**（所有良品图共用，模型要求同一裁图尺寸）。改过尺寸或位置的ROI在绑定时一并写回配方，
  否则位置相关模型会因尺寸不符（`anomaly_model_size_mismatch`）被阻断。“本图框复位”取消本图的移动。
- **运行**：`OpenCvInspectionBackend(anomalyModels: …, anomalyDetectors: …)`。手工特征实现内置；CNN特征模型需宿主按特征来源提供`OpenCvCnnPatchAnomalyDetector`
  （演示程序：设置环境变量`DP_LABEL_ANOMALY_BACKBONE`为骨干网络ONNX路径，新训练的模型即用CNN特征）。
- **报告**：`patch_anomaly`（NG，原图坐标）、`anomaly_summary`（OK说明）、`RegionInspectionResult.Anomaly`（模型、裁图范围、最大得分、阈值、热力图）；
  保存任务时`anomaly-models.json`记录所用模型的库、版本、键和哈希。
- **前提失败（本ROI NG）**：`anomaly_model_unbound`未绑定、`anomaly_repository_unavailable`宿主未提供模型库、`anomaly_model_missing`版本或模型键不存在、
  `anomaly_feature_unavailable`缺少对应特征实现、`anomaly_model_size_mismatch`位置相关模型的裁图尺寸与训练不一致（ROI已改变，需重新训练）。

## 逐字符模式（内容可变的文字）

整ROI的位置相关模式要求每个位置的内容固定；与位置无关模式能接受任意组合，但断笔、笔画内空洞会被别处正常的笔画端点“解释”掉（实拍漏检断开的“6”）。
逐字符模式把问题换成“这个字和良品中的同一个字比”，与字符出现在哪个位置、与哪些字相邻无关。

**归一化**（`CharacterCells`）：每行按OCR身份测量大写字母/数字的顶线与基线（中位数，按整行Otsu墨迹），把大写高度缩放到32像素，
单元高度按行高上下各留0.45倍；水平以分割单元中心为中心、宽度按该字符训练样本的最大单元宽度加左右各0.35倍行高，单元外的列涂成纸色。
按整行几何而不是按每个字自己的墨迹框缩放：缺了顶部或底部笔画的字不会被放大“补齐”，缺陷保持在原位置；相邻字符不进入模型，字距变化不影响。
只有x高度小写字母的行按x高度≈0.7倍大写高度估计。

**训练**：同一字符的所有样本（跨行、跨图）训练一个位置相关模型（单元内±1像素），每字最多16个样本（超出时均匀抽取）；
阈值按留一法最大得分×1.5，并取不低于本批各字符阈值的中位数（至少5个字符时）——样本少的字符留一法阈值不可靠。
条目范围为`EAnomalyModelScope.Character`，键为字符本身（区分大小写），一批字符模型用`PutAnomalyModels`作为一个新版本发布。

**检测**：`AnomalySettings(库, 版本, perCharacter: true)`，仅文字ROI。字符身份与分割：方法A执行过时复用其分割，否则按OCR读数（显式等格用格位标签）重新分割，
因此逐字符模式需要OCR（等格除外）。每个字母/数字字符与其模型比较，异常区域以原图坐标报告并标明字符与位序；
库中没有某字符的模型时该字`anomaly_character_model_missing`判NG（与单字库缺参考相同），配置了引导值时在前提检查阶段就指出缺哪些字符；
分割不可靠时`anomaly_segmentation_failed`（未完成，NG）。热力图为各字符热力图映射回ROI后的合成图（128对应各字符自己的阈值），
`RegionInspectionResult.Anomaly`中最大得分为最差字符的阈值倍数、阈值记为1。

**制作界面**：WinForms工作台“批量训练(B)”（见下文“批量训练页”）。文字行框住整行即可，提取时按OCR（或确认文本）自动分割为字符；
点选框可核对各字符身份（可改）、取消个别字符；字符按行几何归一化，良品图不必与配方对齐。
训练时按字符汇总全部行的样本；每个字符样本多于16个时按形态多样性选取（贪心最远点），保留少数形态不同的良品
（例如另一行字号略不同的同一字符），按顺序等间隔抽取会漏掉它们并造成误报。

**实拍标签评估**（手工特征，±1像素，每字≤16样本；良品8/10/7三折交叉验证，每张约170字、42个字符类别）：

| | 良品误报（字） | 损伤标签5 | 斑驳标签6 | 褪色标签4 | 耗时 |
|---|---|---|---|---|---|
| 逐字符（阈值取中位数下限、多样性选样） | 标签8、10为0；标签7为“N”真实小缺口，以及只有2个样本的“c”“h”，另1个无模型字符（“8”只出现在标签7） | 断开的“6”4.16倍 | “9”3.51倍、CUST PART的“T/R/A/P” | 143字 | 训练全部字符2.5 s；检测约1.5 s/张（约9 ms/字） |
| 同上、等间隔抽样 | 另有“WF675907”中的“0”每张1.4–1.5倍（该行的“0”形态略不同，被抽样漏掉） | — | — | — | — |
| 逐字符（不设下限） | 每张2–4（样本2–4个的“m”“c”“h”） | 另检出“B”1.06倍 | 同上 | — | — |
| 逐字符、CNN特征 | 每张0–3 | 漏检断开的“6” | “9”仅1.08倍 | 62字 | 慢约3倍 |

结论：内容可变的文字用逐字符模式（手工特征）；每个字符至少3个、最好10个以上良品样本；罕见字符（如只在个别标签出现）需专门补样本，否则该字判NG。

上表为修正位置相关上下文相位（见下文“位置相关模式的上下文相位”）之后的结果；修正前断开的“6”只有1.90倍、“9”2.41倍。

## 批量训练页（多图、每图多框、一次训练）

WinForms工作台“批量训练(B)”，逻辑在Core的`AnomalyTrainingSession`（与界面无关、可测试），采集文件由Storage的`AnomalyTrainingProject`读写。

1. **良品图**：“添加图像…”一次选多张；工作台当前图像自动加入。
2. **模型**：打开时为配方中每个ROI建同名模型（固定/空白区和有固定引导值的文字为“内容固定”，其余文字为“逐字符”，码为“内容可变”，可在列表中改），
   也可“新建模型”（与配方ROI同名时对应该ROI，否则只发布模型不绑定）。多个逐字符模型共用一套字符模型（一个模型库对应一种字体）。
3. **框样本**：右侧选中模型后在图上框外拖动即新增样本框，每张图可以框多个、同一模型也可在同一图上框多处；拖动框内部移动、边/角调整，Delete删除。
   “按配方位置放到本图/全部图”一次放好所有对应配方ROI的框，再逐图检查。
   - 内容固定：第一个框（或对应ROI）决定尺寸，后加的框自动改为同尺寸，并在±16像素内按与第一个样本的归一化相关对齐——手画粗略即可；
     改任一框的尺寸则该模型全部框改为新尺寸。**裁图会影响训练**：位置相关模型要求各样本内容在框内同一位置（±3像素内），所以才统一尺寸并自动对齐。
   - 内容可变：框的尺寸和位置不限，框住内容即可。
   - 逐字符：框住整行，自动提取字符（后台依次进行）；点选框在下方核对身份、取消个别字符，或填确认文本重新提取。右下角按字符统计样本数（少于3个标红）。
4. **训练并发布**：训练前列出问题（没有样本的模型、未提取或需复核的行、样本偏少的字符）；一次训练全部模型，作为模型库的一个新版本发布。
   关闭时可把对应配方ROI一键绑定到该版本并启用B（逐字符模型选逐字符模式；内容固定模型尺寸与配方不同时按中心改为模型尺寸）。
5. **保存/打开采集**：图像路径、模型、全部框、逐字符行核对后的身份与取消的字符都保存，下次打开继续采集；没有文件路径的图像另存为PNG。

训练页在工作台运行期间保留；每次打开都与当前配方同步：之后新画的ROI自动加入模型列表，已有模型关联到最新的ROI配置，人工删除的模型不再自动加回
（“从配方导入”可手动加回）。

**ROI编辑中手动绑定**：“绑定所选异常模型库/版本”会按库中实际内容选择模式——库中有本ROI的整ROI模型则用整ROI；文字ROI而库中是字符模型则自动选“逐字符检查”；
都没有时立即提示库中有哪些模型。检测时缺模型的提示（`anomaly_model_missing`）同样列出库中内容，例如文字行误用整ROI模式绑定字符模型库时提示改为逐字符检查。

原“字符异常模型(B)”窗口由批量训练页取代；“异常模型库(B)”窗口保留，用于管理模型库（版本、导入导出、归档、删除模型）和按ROI快速训练。

## 位置相关模式的上下文相位

位置相关模式在±半径内逐个整像素偏移比较；每个块除原尺度8×8外还有1/2尺度的上下文块。修正前良品的上下文块总取相位0的下采样平面，
奇数偏移时与待检块错开半个下采样像素，锐利边缘处得分虚高——良品整体平移1像素就被判异常（合成图上6倍阈值）。
过去手选的良品本身带有各不相同的亚像素偏移，留一法阈值也被同样抬高，问题被掩盖；批量训练页自动对齐样本后阈值变紧，问题暴露。
现在良品的上下文按与偏移同相位的2×2平均得到，任意整像素平移都精确对上。同时，裁图边缘一个块宽的范围不计入最大得分、热力图和留一法标定
（与异常区域一致；此前可出现“最大得分超过阈值但没有异常区域”）。修正后实拍标签（整ROI位置相关）：良品误报与修正前相当，
断开的“6”从1.73倍升至1.93倍、CUST PART的斑驳从2.32倍升至2.47倍。

## 演示

```
DP.LabelInspection.Demo.WinForms.exe --anomaly-demo 配方.json 良品目录 待检目录 输出目录 [骨干网络.onnx]
```

给出第6个参数时使用CNN骨干网络特征，否则使用手工特征。

按配方每个ROI训练一个模型（图像须已与配方对齐，如固定相机），逐张检测后输出：
`<图名>-anomaly.png`（橙色热力、红框异常、蓝框ROI）、`summary.csv`（每图每ROI：判定、异常数、最大得分、阈值、倍数）、`models/<ROI>.dppa`。

## 实拍标签评估

五张实拍标签，16个文字ROI各一个模型。良品只有3张（8、10、7），因此：误报用“两张训练、第三张检测”三折交叉验证；检出用三张全部训练。

| 模式（余量1.5） | 良品误报区域（3折） | 损伤标签5：断开的“6” | 斑驳标签6 | 褪色标签4 | 训练/检测 |
|---|---|---|---|---|---|
| 位置相关±3 | 2（其中1处为标签7“N”上的真实小缺口） | 检出，1.73倍 | “9”及CUST PART的“A/R/T”检出，最高2.32倍 | 130处，最高3.6倍 | 15 s / 约7 s（16个ROI） |
| 与位置无关 | 0 | 漏检 | 1处弱检出 | 19处 | 67 s / 约3 s |

结论：

- 内容固定的ROI用位置相关模式，可检出单字比较能发现的缺墨/断笔，且不需要分割和字库。
- 与位置无关模式几乎不误报，但缺笔画、笔画内空洞与别处正常的笔画端点/边缘相似，会被“解释”掉，只适合发现严重损伤或纸面异物。
- 良品越多阈值越可靠；3张良品时个别ROI阈值偏紧（标签8的“LOT ID”一处1.07倍）。生产建议每个配方至少10–20张良品，良品来自多个批次/多台打印机。

### CNN骨干网络特征（位置相关±3，余量1.5，同样的交叉验证）

| | 良品误报区域 | 损伤标签5 | 斑驳标签6 | 褪色标签4 | 训练/检测 |
|---|---|---|---|---|---|
| CNN骨干网络 | 2（均在标签7“Xi'an”行：“N”小缺口与“c”旁斑点，真实小缺陷）；标签8、10为0 | 15处（含断开的“6”1.19倍、“ISP(HLIC)-B-0233”中破损的“B”） | 15处（含“SILICON WAFER”中“A”的空洞、CUST PART多处） | 222处 | 5 s / 约3 s |
| 手工特征 | 2（含标签8“LOT ID”一处1.07倍） | 4处（断开的“6”1.73倍） | 7处（含“9”） | 130处 | 15 s / 约7 s |

CNN特征误报更少、速度更快，并检出了手工特征漏掉的“A”空洞；但漏检标签6中“WF675907”的斑驳“9”。两者互补，可按ROI选择或同时运行取并集。

## 兼容性检查（三轮）

1. **框架/构建**：DP.Vision.OpenCv与DP.LabelInspection.Runtime在net48与net8.0-windows下0警告编译；同一程序在Mono（.NET Framework 4.8）与.NET 8上运行，
   两种特征、两种模式的阈值与得分到小数点后6位一致。
2. **数据/格式**：模型格式升为版本2（增加特征来源），版本1文件仍可读取（视为手工特征），未知版本明确拒绝；手工/CNN模型互不接受，放大倍数不同也拒绝；
   OpenCvSharp对多维Mat的`SetArray/GetArray`会静默不复制（首次接入时输出全为0），CNN特征改用`BlobFromImage`与指针复制，并用与ONNX Runtime对比的数值验证；
   CNN位置相关模型改为保存灰度裁图，体积缩小约36倍，结果与保存特征网格时完全一致。
3. **运行时/原生**：确认Windows x64/x86原生库包含所需DNN导出；同一检测器并发检测结果与串行一致（骨干网络前向串行化，缓存线程安全）；
   极小裁图（6×5）与彩色裁图正常处理；缺失的骨干网络文件、输出数不为2的模型在构造时明确报错。

## 限制

- 图像须与训练时对齐；位置相关模式要求ROI尺寸与训练一致，ROI改动后需重新训练。
- ROI四周要留余量，内容贴边被截断会产生异常。
- 与训练良品成像条件（焦距、曝光、分辨率）差异大时会整体升高得分，需要用新条件的良品重新训练。
- 模板模式下B在定位（整图平移）后的ROI上检测；自由模式要求图像与训练时对齐（固定相机/工位）。
- 逐字符模式依赖OCR身份与分割：OCR读错（如O/0）时按读到的字符的模型比较，通常会报异常；分割失败时不能逐字判断。
- 位置相关模式的得分用积分图按“良品×偏移”整体计算（DP.Vision），与逐块搜索结果一致，耗时与良品样本数×偏移数成正比。
