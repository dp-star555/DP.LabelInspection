# ROI参数中文说明

入口：原生WinForms工作台 → **编辑ROI/规则** → 左侧选ROI → 右侧选参数。

## 本次界面修改

- 27个可编辑属性全部提供中文显示名和说明，按“检测项目、区域位置与类型、文字分割、单字外观、内容约束、码制声明、条码印刷质量”分组。
- “读取实际数据”“检查印刷质量”是当前项目配置。已删除重复的“执行条码印刷检查”开关；码质量是否执行只由“检查印刷质量”决定。
- 旧配方没有Tasks时从旧印刷开关迁移；存在显式Tasks时以Tasks为准，并同步旧兼容字段，不要求用户维护两个开关。原生WPF的ROI项目栏使用相同配置规则。
- 组名标明“仅文字/仅条码/文字与条码”，避免混淆适用对象。
- 布尔值显示“是/否”，检查类型和条码类型显示中文；底层仍保存原布尔值/枚举，不修改SDK字段名或JSON格式。
- 底部使用独立的130像素高、多行可滚动说明区。选中参数即显示用途、范围、默认值、单位、调节方向及适用限制，而不是只显示英文属性名。
- 保存配置才应用；“取消修改”或关闭窗口不应用编辑。
- 此次仅调整参数界面，没有改变算法、默认阈值或判定策略。

## 最容易混淆的参数

| 界面中文 | SDK属性 | 含义 |
|---|---|---|
| 单字膨胀容差（归一化像素） | GlyphTolerance | 0–8，默认2。0关闭膨胀豁免，但不关闭归一化或±2像素对齐。调大更宽松，可漏掉细长差异。 |
| 最大允许字形差异比例 | MaximumDifference | 0–5，默认0.18即18%。容差后缺墨与多墨之和除以归一化参考墨迹面积；调小更严格。不是OCR置信度。 |
| 最低OCR置信度 | MinimumConfidence | 0–1，默认0.75。普通OCR身份低于此值可阻断字形比较；不是字形合格概率。 |
| 按等宽单元切字 | EqualCells | 默认否。仅当真实排版确实等宽、顺序已明确时开启；不是通用粘连修复开关。 |
| 条码边缘排除（原图像素） | BarcodeEdgeTolerance | 0–8，默认1。仅条码印刷检查使用，与单字膨胀容差不同。 |
| 最小异常面积（原图像素²） | BarcodeMinimumArea | 1–1000000，默认4。按一维候选或QR模块内部差异面积判断。 |
| 最小异常面积比例 | BarcodeMinimumFraction | 0–1，默认0.01。分母为当前条/空隙或QR模块的检查面积，不是整个ROI。 |
| 最小墨色损失比例 | MinimumInkLoss | 大于0且不超过1，默认0.25。一维条码灰度变浅补充检查，调小更敏感。 |
| 字库类别ID / 字库版本 | LibraryId / LibraryRevision | 建议通过下方下拉框绑定；固定修订，不会随字库补字自动升级。 |

## 验证

- 修改前原生回归明确失败：`ROI parameter lacks Chinese name/help: Name`，证据`artifacts/roi-chinese-red.png.error.txt`。
- 两运行时遍历全部可编辑属性，断言中文名称与非空说明。
- 原生属性窗口探针选中膨胀容差，检查底部说明实际可见，并包含0关闭、5×5、不是原图像素等关键提示。
- 检查“是/否”、中文类型的双向转换；保存容差0后，区域类型与其他默认值保持不变。
- 日志`artifacts/roi-chinese-verification.log`：170项/框架及原有实际OCR/OpenCV、原生工作台流程通过。
- 截图：`artifacts/winforms-net48.png.quick-library.png.parameters.png`、net8对应文件。使用合成ROI配置，不含生产图或用户字库内容。

运行当前源码`start-ocr.cmd`使用；旧dist未重新发布。此参数编辑页为WinForms，不声称WPF已有同等编辑页。
