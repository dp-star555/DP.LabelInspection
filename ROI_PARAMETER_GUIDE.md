# ROI参数中文说明

入口：原生WinForms工作台 → **编辑ROI/规则** → 左侧选ROI → 右侧选参数。

## 本次界面修改

- 31个可编辑属性全部提供中文显示名和说明，按“检测项目、区域位置与类型、文字分割、单字外观、内容约束、码制声明、条码印刷质量、异常检测（方法B）”分组。
- 检测项目：“读取实际数据”“A 规则质检（按类型）”“B 局部异常检测（良品模型）”。A、B可单独或同时启用，同时启用时任一NG即NG；B需在“07 异常检测”绑定异常模型库的固定版本（下方“绑定所选异常模型库/版本”），模型键留空即用ROI名称。
- “读取实际数据”“检查印刷质量”是当前项目配置。已删除重复的“执行条码印刷检查”开关；码质量是否执行只由“检查印刷质量”决定。
- 旧配方没有Tasks时从旧印刷开关迁移；存在显式Tasks时以Tasks为准，并同步旧兼容字段，不要求用户维护两个开关。原生WPF的ROI项目栏使用相同配置规则。
- 组名标明“仅文字/仅条码/文字与条码”，避免混淆适用对象。
- 布尔值显示“是/否”，检查类型和条码类型显示中文；底层仍保存原布尔值/枚举，不修改SDK字段名或JSON格式。
- 底部使用独立的130像素高、多行可滚动说明区。选中参数即显示用途、范围、默认值、单位、调节方向及适用限制，而不是只显示英文属性名。
- 保存配置才应用；“取消修改”或关闭窗口不应用编辑。
- 此次仅调整参数界面，没有改变算法、默认阈值或判定策略。

## 最容易混淆的参数

| 界面中文 | SDK属性 | 含义 |
|---|---|---|
| 单字边缘容差（归一化像素） | GlyphTolerance | 0–8，默认2。只触及笔画边缘带（本值，整体偏粗/偏细时自动加宽）的差异视为印刷波动；深入笔画、贯穿笔画或整段缺失的差异按完整面积计入。设0时差异仍须深入笔画才计入，不关闭归一化及对齐。调大更宽松，可漏掉浅的边缘缺口。 |
| 最大允许字形差异比例 | MaximumDifference | 0–5，默认0.05即5%（旧版本默认0.18，已保存的配方保持原值）。计入的缺墨与多墨之和除以归一化参考墨迹面积；边缘波动不计入，良品通常为0或接近0，可用一批良品实测后留余量设较小阈值。调小更严格。不是OCR置信度。 |
| 最低OCR置信度 | MinimumConfidence | 0–1，默认0.75。普通OCR身份低于此值可阻断字形比较；不是字形合格概率。 |
| 按等宽单元切字 | EqualCells | 默认否。仅当真实排版确实等宽、顺序已明确时开启；不是通用粘连修复开关。 |
| 条码边缘排除（原图像素） | BarcodeEdgeTolerance | 0–8，默认1。条/空隙或QR模块交界的边缘带：只触及边缘带的毛刺、渗墨不计入，深入内部的缺陷按完整面积计入；一维条上下端从条端开始且不超过条高10%的缩短/渐淡视为条长波动；细条只检查横贯断裂。与单字边缘容差不同。 |
| 最小异常面积（原图像素²） | BarcodeMinimumArea | 1–1000000，默认4。按一维候选或QR模块内部差异面积判断。 |
| 最小异常面积比例 | BarcodeMinimumFraction | 0–1，默认0.01。分母为当前条/空隙面积或一个QR模块面积，不是整个ROI。 |
| 最小墨色损失比例 | MinimumInkLoss | 大于0且不超过1，默认0.25。一维条码灰度变浅补充检查，调小更敏感。 |
| 字库类别ID / 字库版本 | LibraryId / LibraryRevision | 建议通过下方下拉框绑定；固定修订，不会随字库补字自动升级。 |
| 异常模型库ID / 版本 / 模型键 | Anomaly.LibraryId / LibraryRevision / ModelKey | 方法B的模型绑定，与字库相同按固定修订；重新训练发布新修订后需重新绑定。模型键留空即ROI名称。 |

## 单字差异阈值实测（默认0.05的依据）

五张实拍标签（参考为较干净的一张），同一ROI同一字位与参考单字比较，深度边缘带比较器默认参数，序列号末4位（各标签内容不同）不计：

| 标签 | 中位数 | 99分位 | 最大值 | 超过0.05 |
|---|---|---|---|---|
| 干净 | 0.000 | 0.002 | 0.025（N右上小缺口） | 0 |
| 局部斑驳 | 0.000 | 0.122 | 0.133（“1”大片缺墨） | 4（“1”“9”“T”“5”，均为肉眼可见缺墨） |
| 明显损伤 | 0.000 | 0.035 | 0.244（断开的“6”） | 1 |
| 严重褪色 | 0.075 | 0.902 | 0.979 | 107 |

旧默认0.18只检出断开的“6”，斑驳标签上肉眼可见缺墨但仍可识读的字（0.06–0.13）全部放行；0.05在干净标签最大值之上留一倍余量，并检出上述缺墨字。
合成小缺陷（半径2–3像素空洞、细裂纹，约0.01–0.03）仍低于0.05，需要检出此级别时按良品实测再调小。已保存配方的阈值不会自动改变。

## 验证

- 修改前原生回归明确失败：`ROI parameter lacks Chinese name/help: Name`，证据`artifacts/roi-chinese-red.png.error.txt`。
- 两运行时遍历全部可编辑属性，断言中文名称与非空说明。
- 原生属性窗口探针选中单字边缘容差，检查底部说明实际可见，并包含边缘带、完整面积、不是原图像素等关键提示。
- 检查“是/否”、中文类型的双向转换；保存容差0后，区域类型与其他默认值保持不变。
- 日志`artifacts/roi-chinese-verification.log`：170项/框架及原有实际OCR/OpenCV、原生工作台流程通过。
- 截图：`artifacts/winforms-net48.png.quick-library.png.parameters.png`、net8对应文件。使用合成ROI配置，不含生产图或用户字库内容。

运行当前源码`start-ocr.cmd`使用；旧dist未重新发布。此参数编辑页为WinForms，不声称WPF已有同等编辑页。
