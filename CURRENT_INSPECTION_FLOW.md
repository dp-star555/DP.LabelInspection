# 当前检测流程（逐ROI源码实现）

本文件描述源码，不代表旧 `dist` 已更新。原整体分析流程已归档到 `docs/decision-flow/archive-v1/CURRENT_INSPECTION_FLOW.monolithic.md`。

## 逐步总览（从配置到判定）

以下按实际执行顺序列出；每一步只在前一步通过后执行。后文各节是每一步的详细规则。

### 第0步：准备（离线，一次性）

1. **配方**：人工框选ROI（文字/条码/固定/空白/忽略），每个ROI选择检测项目：“读取数据”，以及两种可单独或同时启用的质量方法——“A 规则质检”（按类型，第6步）和“B 异常检测”（良品模型，第6B步）；可设引导值、格式约束和跨ROI/任务绑定。
2. **单字库**（文字质量用）：多图补录制库；每个字保存原始灰度图和二值化模式（`otsu`默认 / `midpoint`中点 / `fixed`固定阈值）。
3. **异常模型库**（B用）：与单字库一样按不可变版本管理；用若干已对齐的良品图为ROI训练模型并发布新版本，ROI绑定“模型库ID+固定版本(+模型键，默认ROI名)”，不自动升级。

### 第1步：前置检查（每个ROI）

边界、项目冲突（如设了引导值却关闭读取）、字库修订、OCR能力；启用B时还检查模型库绑定、固定版本与模型键存在、特征实现可用（CNN模型需宿主提供骨干网络）、位置相关模型的裁图尺寸与训练一致。失败即该ROI为NG，不执行后续步骤；其他ROI继续。

### 第2步：取得引导数据

解析任务数据或其他ROI的实际读数（依赖的ROI先执行，检测循环依赖）。

### 第3步：定位

模板模式做整图对齐；自由模式直接使用框选位置。

### 第4步：读取

文字ROI执行OCR，条码ROI解码。读不到、多个结果、可信度过低、码制不符 → NG，不执行后续。

### 第5步：内容比较

设了引导值或格式约束时，实际读数必须严格一致；不一致 → NG，不执行印刷质量。

### 第6步：印刷质量A——规则质检（按ROI类型，启用A时）

| 类型 | 逐步做法 |
|---|---|
| 文字 | ①字符假设：OCR读数（盲测）；有引导值时第5步已保证二者一致；显式等格用引导值。②分割：投影/连通域分组，与字符数不一致时按字符数与字宽先验求最优切割（`count_guided_cuts`），穿墨过深或字宽不合理则拒绝。③配对：每个字匹配单字库参考。④单字比较：实际字与参考用同一二值化模式与参数处理，边缘带内波动不计，深入笔画的缺墨/多墨按完整面积计入，差异与阈值（默认0.05）比较。⑤输出逐字结果与`text_quality_summary`汇总。 |
| 一维码 | 条/空隙结构检查：边缘带与条端区（条高10%）不计；内部≥4像素的条才做灰度变浅检查；指示性扫描等级；经修复预处理才读出时报告`barcode_decode_assisted`。 |
| QR | 解码得到模块网格，逐模块检查缺墨/多墨，考虑整体墨迹扩散；可选静区检查。 |
| 固定/空白 | 与参考图比较缺墨/多墨，或空白区域污点。 |

### 第6B步：印刷质量B——局部块异常检测（启用B时）

与A并列、互不依赖。整ROI模式不需要OCR、分割或字库，适用于文字、条码、固定和空白ROI：在定位后的ROI（四周加训练时的边距）上与绑定模型的良品块比较，
超阈值区域输出`patch_anomaly`（原图坐标），另输出`anomaly_summary`（OK，仅说明：模型、特征、模式、最大得分/阈值倍数、异常区域数），
报告的`RegionInspectionResult.Anomaly`保存得分和热力图。A已发现缺陷时B仍执行，便于对照两种方法的证据。

逐字符模式（内容可变的文字）：按字符分割（A执行过时复用A的分割，否则按OCR读数分割，因此需要OCR），每个字按整行字高归一化后与库中同一字符的模型比较；
缺陷标明字符与位序，库中缺某字符的模型时该字NG。

| 启用 | 质量判定 |
|---|---|
| 仅A | A的结果（与以前相同） |
| 仅B | B的结果 |
| A+B | 两者都执行；任一NG或任一未完整执行即NG |
| 都不选且不读数据 | 配置错误`no_roi_tasks`，NG |

### 第7步：判定与报告

任一ROI有NG发现即整体NG；OK级说明（如文字汇总、异常检测摘要）不影响判定。报告保存每个ROI的阶段轨迹、读数、发现、逐字证据和B的得分/热力图；
保存任务时另记录所用异常模型的库、版本、键与哈希（`anomaly-models.json`，不复制模型字节）。

### 第8步：保存与复判（由宿主决定）

检测控件只发出`InspectionCompleted`。宿主可`SaveReport`保存完整任务；操作员复判用`AddFeedback`（整任务）或`AddRegionFeedback`（单个ROI）；`SampleExporter`把已保存任务导出为带人工标签的逐ROI样本集，用于回放评估和以后训练。

### 异常模型（B）的训练与模式

只用良品训练。整ROI模式按ROI一个模型：固定内容用位置相关模式（与良品同一位置±3像素比较）；可变文字用逐字符模式（每个字符一个模型、多个良品样本）。
特征可选手工特征或CNN骨干网络（OpenCV DNN运行；逐字符模式推荐手工特征）。
WinForms工作台“异常模型库(B)”按ROI训练，“字符异常模型(B)”从多张良品图提取字符、核对身份后按字符训练；发布后均可一键绑定到新版本并启用B。详见`ANOMALY_DETECTION.md`。

## 工程边界

不再保留四个旧算法工程或其程序集：`Barcode.Zxing / Ocr.Onnx / Vision.OpenCv / Vision.OnnxDetection`。标签业务装配集中到 `DP.LabelInspection.Runtime`；通用算法在 `DP.Vision.*`，实际DB候选检测也已迁到 `DP.Vision.OnnxDetection`。旧命名空间已移除，没有类型转发，宿主必须更新引用。

## 正式入口

`InspectionEngine.Inspect` → `IRoiWorkflowBackend.OpenSession` → `Core/RoiWorkflow.Run`。
内置 `OpenCvInspectionBackend.Analyze` 兼容入口也委托同一逐ROI流程，不再维护另一套文字/码混合检测流程。

对每个非 Ignore ROI：

1. 读取 `Tasks.ReadData / Tasks.CheckQuality（A）/ Tasks.DetectAnomaly（B）` 和所选策略依赖。
2. 校验边界、冲突、必需的整图参考、识别器、字库固定修订等已知条件；启用B时由会话的`IRoiAnomalySession.ValidateAnomaly`校验模型绑定，会话不支持B则`anomaly_unavailable`。失败记录当前ROI NG，不运行后续算法。
3. 解析本周期任务引导值或跨ROI实际读数。跨ROI依赖按需执行一次，检测循环；报告仍保持配方顺序。
4. 必要时定位。模板模式的 Translation 才执行整图ECC；Free模式不因旧默认Translation枚举值强迫加载整图参考。
5. 需要数据或质量身份时，实际OCR/读码；读不到、多码、低可信度、类型不符均阻断当前ROI。
6. 配置引导值时严格比较实际读数，再检查格式约束。不得把引导值写回识别结果，不进行O/0或大小写修正。失败不执行当前ROI质量。
7. 执行已选择的质量方法：先A（`InspectQuality`），再B（`InspectAnomaly`），两者的发现合并到本ROI；空 findings 不能证明完成；任一方法显式未完成为NG（`quality_incomplete`/`anomaly_incomplete`）。
8. 保存阶段轨迹，继续其余ROI。运行异常归属实际失败阶段；用户取消向上传播，不伪造完成报告。

没有有效检测ROI、检测ROI的数据/A/B三个项目均关闭，均不得空跑后放行。正式验收不再自动发现ROI；检测器仍可用于明确的配方准备/候选发现调用。

## 数据与质量独立

| 类型/配置 | 数据 | 印刷质量 |
|---|---|---|
| 固定/空白 | 默认不适用 | 固定缺墨/多墨或空白污点 |
| 普通文字仅数据 | OCR及已配置引导/格式约束 | 未配置，不因没做外观自动降成REVIEW |
| 普通文字字库质量 | 默认策略需要实际OCR身份 | 物理分割→独立配对→单字比较 |
| 显式等格文字 | 格位标签本身不是OCR证据；可额外选择读取 | 按明确格位声明与固定修订参考比较 |
| 显式一维码仅质量 | 默认策略不要求解码 | 独立条纹/墨迹质量 |
| QR质量 | 当前实现需要解码网格结构 | 独立QR质量，不回退到一维条纹 |
| 替代整段文字质量 | 策略声明是否需要识别 | 可不依赖分割/字库；必须声明完成，不能伪造字符证据 |

### 字符假设（盲测与已知数据）

普通文字字库质量的分割与配对以字符假设为准：

- 盲测（未配置引导值/绑定）：以OCR读数作为字符假设，按其字符数分割。OCR身份不是业务真值；报告中明确标注“盲测假设”。
- 已知数据（固定引导值或跨ROI/任务绑定）：第6步严格比较通过后才执行质量，此时字符假设就是已核对的数据；比较失败不执行质量。
- 显式等格：以引导值和格位声明为准，不依赖OCR。

投影分组数与字符假设不一致（粘连、断字、标点）时，分割按字符数与字宽先验求最优切割（依据`count_guided_cuts`），报告切过墨迹的位置与深度；穿墨过深或字宽明显不合理时拒绝，不强行凑数。

质量完成后另输出`text_quality_summary`（OK，仅说明）：字符假设及来源、分割依据、每字差异/内部缺墨/多墨像素与阈值，便于OK时也能查看余量；判定仍由逐字结果决定。逐字未通过的`glyph_exceeds_threshold`等结果给出位序、差异与阈值、缺墨/多墨像素。

引导值要求真实读取；除显式等格标签语义外，关闭读取同时保留引导/数据绑定属于配置矛盾。数据通过不认证印刷质量；质量NG也不会改写已取得的原始读取证据。

不运行强制整图清晰度/对比度门控，不把已测得的NG因为未运行整图评价降成REVIEW。报告原整图指标为NaN，UI显示“未执行”，不是零分。

## 策略与资源

- 中立接口在 `DP.Vision.Algorithms`，真实实现位于 `DP.Vision.OpenCv / Onnx / Zxing`。
- `WithQualityAlgorithms(...)` 装配固定/空白策略，支持整段文字策略和匹配器；分割、单字比较、读码、QR与一维码质量仍可分别注入。
- 算法选择由宿主代码装配；当前配方持久化数据/质量项目和参数，不包含任意插件加载器或通用模型选择UI。
- 标签业务类型仍需与中立视觉证据转换，但不再为被删除的旧工程提供程序集/命名空间兼容。**第三方仅实现旧 `IInspectionBackend` 的后端仍走历史兼容通道**；必须实现 `IRoiWorkflowBackend` 才具有这里的前置读取/质量门控保证。
- 单字库独立、ASCII大小写敏感、固定修订；补录必须人工核对发布，不自动替换参考。制库候选补切不进入正式分割。
- HALCON算法实现、ISO码评级、通用深度质量模型未提供；接口可替换不等于这些算法已经交付。

## 配置、证据与界面

`InspectionRegion.WithTasks(...)` 保存项目，`WithAnomaly(...)`保存B的模型绑定；旧配方缺少Tasks时从原类型/字库/等格/码印刷开关恢复默认值，缺少B字段时B为未启用、无绑定。显式Tasks是唯一项目依据，自动同步旧码印刷Enabled兼容字段；已删除UI重复的条码质量开关。JSON往返和ROI移动保留项目。

WinForms中文ROI编辑器提供读取、A规则质检、B异常检测开关及异常模型库绑定；原生WPF的单ROI项目栏提供读取/A/B开关（B的模型绑定需在配方或WinForms中设置）。WPF并非全部WinForms编辑器功能等价。

`RegionInspectionResult.Execution` 分别记录前检、读取、比较、质量：通过、失败、未配置/不适用、被阻断未执行。每ROI一个F父项，全部原始子项保留；等长内容错误可显示字符串偏移差异，但不冒充物理字符缺陷框。阻断记录与定位候选计数分开；适配器保留中立算法的显式 `IsExecutionBlocker` 角色，自定义阻断码即使带范围框/面积也不冒充印刷缺陷点。

原始图像不修改，原像素边缘坐标不改变；中立租约由结果显式拥有，旧报告适配在释放前复制图像。当前没有零拷贝、工业精度、实际相机吞吐或WPF物理输入通过的声明。

## 验证入口

- `tests/DP.LabelInspection.Tests/Workflow/RoiWorkflowTests.cs`：调用顺序、当前ROI早停、后续ROI继续、依赖、未完成、配置往返。
- `tests/DP.LabelInspection.Tests/Text/IndependentTextQualityTests.cs`：无字库/无OCR的替代质量策略以及旧Analyze入口同流程。
- `../DP.Vision/tests/DP.Vision.Algorithms.Tests/Text/TextQualityTests.cs`：真实分割/比较组合、替代匹配器、整段质量完成状态。
- 完整验收：两项目 `verify.ps1`；最新结果见各自 `VALIDATION.md`。
