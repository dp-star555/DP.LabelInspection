# QR Code打印墨迹检查

ROI下拉框现在有“二维码（QR）”“一维条码”“条码（自动）”。QR与一维仍共用打印质量开关，但使用独立算法。SDK以`ERegionKind.Barcode`＋`FieldSettings.BarcodeType=EBarcodeKind.QrCode`声明QR；旧配方默认Auto。DataMatrix、Aztec、PDF417等不在本次QR实现范围。

## 内容与外观分离

- ZXing继续执行原始二维码解码、Expected/绑定校验。
- 解码器额外尝试提取观察到的QR网格，并独立解码核对其内容与当前唯一符号一致。
- 通过`BarcodeObservation.ModuleGrid`携带原图四角、模块数及观察到的模块采样；Contracts不暴露ZXing/OpenCV类型。
- `OpenCvQrPrintInspector`将原图像素映射到模块网格，绘制理想模块图后逐像素比较，按连通块判断到模块黑白交界的**深度**：只触及交界边缘带（`BarcodeEdgeTolerance`，整体墨迹扩散/收缩时自动加宽对应方向，上限为模块半宽的40%）的渗墨、毛刺不计入；深入模块内部（达到模块半宽的一半，缺墨以整个模块半宽为上限）的缺墨/多墨按完整面积计入。
- 原图读不出时读码器尝试有限的修复预处理后读取，结果带`Preprocessing`标记并输出`barcode_decode_assisted`（见[BARCODE_PRINT.md](BARCODE_PRINT.md)）。
- 定位图案、分隔区、时序线、校正图案和固定深色模块按QR版本规则建立独立黑白期望，可检查这些位置的整模块翻转。格式/版本信息位与数据区不冒充已核验。
- 其他模块黑白状态由其内部多数像素估计，**不是纠错后的独立标准图**。不将任意重新编码的二维码当成印刷真值，避免掩码、版本、编码分段不同造成错误比较。

## 输出

- `qr_missing_ink` / NG：黑模块内部缺墨候选。
- `qr_extra_ink` / NG：白模块内部多墨候选。
- 缺陷框与`AreaPixels`都是原图坐标/像素；每处差异按连通块报告（可跨模块），消息给出最深处所在模块及其距交界的距离，最多256条。
- `qr_module_review`：部分模块黑白占比接近，不能可靠推断原始状态。
- `barcode_not_decoded` / NG：明确框选码区且解码器实际执行后未读出，未满足本次读取要求；不宣称已经定位全部印刷缺陷。
- `qr_print_unavailable` / REVIEW：明确QR却没有可确认网格，局部外观定位未知，不转入一维检查。父汇总仍由读码失败的NG主导。
- `barcode_print_review`：网格不可靠/缺失、裁切、对比度不足、模块太小或几何与采样不一致。
- `qr_print_scope` / OK：检查范围说明，不等于整个二维码或ISO评级认证。

当前至少需要每模块约4原始像素，并要求各模块内部有足够检查像素。严重透视、镜像/反色或不能可靠恢复网格的符号，即使仍可解码，也可能只能给打印REVIEW；不把读码成功改写成打印合格。

## 参数

在“编辑ROI/规则 → 条码打印质量”中使用现有四项参数：

- `BarcodePrintEnabled=True`：启用。关闭后仅读码及内容约束，不产生未启用打印检查的错误。
- `BarcodeMinimumArea=4`：QR单模块内部聚合差异的最小原始像素面积。一维检查则是单个连通域面积。
- `BarcodeMinimumFraction=0.01`：差异像素占该模块内部检查像素的比例，而不是整个二维码/ROI比例。
- `CheckQrQuietZone=False`：另行开启四周4模块静区检查；关闭时不制造缺失检查错误。开启后ROI须包含完整静区，否则`qr_quiet_zone_review`。污点按`BarcodeMinimumArea`连通域面积判断，不套用`BarcodeMinimumFraction`，避免宽静区稀释小污点。`qr_quiet_zone_ink`为NG原图框/面积，`qr_quiet_zone_scope`说明检查范围。静区外的无关文字不计入。
- `BarcodeEdgeTolerance=1`：根据模块分辨率换算边缘排除范围，上限为模块边长的20%，避免排除整个小模块。

范围之外的墨迹不能据此判定合格；阈值必须按采集分辨率与工艺校准。

## 重要限制

- 支持**模块内部局部缺墨/多墨**及上述**固定结构模块的整模块翻转**；仍排除配置的边缘容差。
- **数据区整个模块被完全翻转、大片共同损坏、模块边缘形变及纠错前后模块差异**没有完整检测保证。
- 不提供ISO 15415等级，不保证任意透视、反色、镜像、低分辨率、严重污损下的打印判定。
- 网格与解码无法交叉确认时，局部外观明细明确REVIEW；明确码区读不出则读取结论NG，两者不能混为一谈。解码器未加载仍是未执行REVIEW；整图探索未发现码也不等于码区读取失败。质量不足不再把已经执行的读取失败NG改成REVIEW，但其他外观候选仍遵守质量降级。
- DataMatrix外观仍明确未支持，不应把QR通过推广为所有二维条码通过。

## 验证

采用实际ZXing/OpenCV执行生成QR图像的测试：正常图、黑模块局部缺墨、白模块局部多墨、90度旋转、原始面积/坐标、大版本带校正图案、低分辨率及透视网格不可靠时保守返回。

增量测试：可解码情况下完整时序黑模块擦除、完整白模块填黑；版本1/7/32/40正常图无结构误报（含版本32特殊校正图案间距）；镜像虽可解码但不对未校正网格套固定规则，保留REVIEW。

静区增量：正常留白、9像素污点框/面积、开关独立、4模块之外的墨迹不误算、紧ROI提示未覆盖、最小面积阈值及配方开关持久化。检查排除配置的码体边界容差，不是无容差的ISO符合性认证。

用户保存的严重污损QR原图/ROI `[548,1106,263,260]` 回放：明确QR后输出`barcode_not_decoded=NG`、`qr_print_unavailable=REVIEW`，汇总NG，不再提示“一维条纹”。标记视图只显示整个读取失败码区，不伪造小缺陷位置。

这些是算法与接口回归，不是实际打印缺陷数据集的工业精度验证。尚无经人工完整标注的用户QR缺陷图评估结果。
